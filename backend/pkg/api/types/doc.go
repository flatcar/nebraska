// Package types holds the domain model shared across the backend: the entities
// Nebraska persists, the status and event constants describing them, and the
// sentinel errors returned by operations on them.
package types
